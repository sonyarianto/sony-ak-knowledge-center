Please see below example. It's simple using vanilla Javascript.

~~~javascript
  var element =  document.getElementById('map');
  
  if (typeof(element) != 'undefined' && element != null) {
    // do some work here if element is exists
  }
~~~
